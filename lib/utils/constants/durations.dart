// Dio
const Duration defaultReceiveTimeout = Duration(minutes: 10);
const Duration defaultConnectionTimeout = Duration(minutes: 10);
const Duration defaultSendTimeout = Duration(minutes: 10);

// Event Transformer
const Duration debounceDuration = Duration(milliseconds: 100);
const Duration throttleDuration = Duration(milliseconds: 100);
